# Arcavex implementation ledger

Statuses: Not started | In progress | Implemented | Tested | Reviewed | Accepted | Deferred by specification | Externally blocked

| Spec section | Requirement | Status | Source | Tests | Acceptance | Review | Blocker |
|---|---|---|---|---|---|---|---|
| §11 Phase -1 | Skia feasibility (SkParagraph, fonts, SkSL, PNG, PDF, determinism) | Accepted | docs/adr/0001, tests (Phase 0 converts probes) | feasibility probe (9/9 pass) | probe script run 2026-07-12 | ADR recorded | — |
| §3.1 | IR: CompiledDocument/LayoutDocument, units, colors, hashing | Accepted (v0). CR-13 resolved Phase 4: canonical numbers unify int/float (2 == 2.0 hash-equal) while numbers stay JSON numbers (2 != "2"); shared serializer feeds template/style/data hashes, manifests, run ids. | src/arcavex/kernel/ir/ | tests/unit/test_canonical.py, tests/property/test_canonical_property.py | canonical property + int/float unification | phase-00 accepted; CR-13 Phase 4 | — |
| §3.2 | Contracts (8 SPIs) | Accepted (v0 contracts; Effect/Mask/Shape/AssetDecoder impls in later phases) | src/arcavex/kernel/contracts/ | tests/unit/test_registry.py | — | phase-00 loop: accepted | — |
| §3.3 | Registry + bootstrap + extension loader | Accepted (registry+bootstrap; local ext loader = Phase 6) | src/arcavex/kernel/registry/, bootstrap.py | tests/unit/test_registry.py | lint-imports | phase-00 loop: accepted | — |
| §3.4 | Pipeline orchestrator | Accepted (v0 compile→layout→render→export sequencing in facade) | src/arcavex/kernel/api.py | tests/e2e/test_cli.py | render CLI | phase-00 loop: accepted | — |
| §3.5 | Observer-only hooks | Not started | | | | | |
| §3.6 | Diagnostics model + codes + explain | Accepted (model+codes+located+hints; explain = Phase 1) | src/arcavex/kernel/diagnostics.py | tests across suites | validate --json failure paths (exit 1/3/4/5 demonstrated) | phase-00 loop: accepted | — |
| §3.7 | Service API facade | Accepted (render_file, validate_template; rest per later phases) | src/arcavex/kernel/api.py | tests/e2e | CLI acceptance | phase-00 loop: accepted | — |
| §4.1 | Template system: loader, expressions, styles, formats/locales/patches, compiler | Accepted through Phase 2 scope (Phase 1 split/repeat/if/functions/schema; Phase 2 formats+locale application incl. direction inheritance, null-as-value, data layering+sidecar, format/locale patches with provenance; styles Phase 3) | src/arcavex/services/template/ | tests/unit (expressions, structural, variables, functions, locales, patches) | validate/render, template inspect --resolved | phase-02 remediation (ADR-0002) | — |
| §4.1.4 | Formats, locales, patches, data overlays + provenance | Accepted (Phase 2; set/remove/insert ops addressing authored IDs incl. root and repeat/if wrappers, unknown-final-segment error, !delete tag-only, PatchLog surfaced by --resolved) | src/arcavex/services/template/overlays.py, compiler.py | tests/unit/test_patches.py, test_locales.py | template inspect --resolved | phase-02 remediation | — |
| §6.1.3+§3.7 | doctor, explain, template authoring commands, watch preview | Accepted | src/arcavex/services/{doctor,explain,authoring}.py, clients/watch.py | tests/unit + tests/e2e | doctor --json, explain, preview --watch session | phase-01 loop: accepted | — |
| §4.2 | Anchor layout solver + fit policies | Accepted (Phase 2; sibling/logical anchors, stacks incl. RTL mirroring, aspect from clamped axis, min/max on all modes, rotation AABB for anchors/overlap, fit policies) | src/arcavex/builtin/layout_anchors/solver.py | tests/unit/test_layout*.py, tests/unit/test_text_fit.py, tests/layout_snapshots | ipen 6 renders + layout inspect | phase-02 loop + remediation | — |
| §4.3 | Text stack (SkParagraph, bundled fonts, BiDi/RTL, locale digits) | Accepted (Phase 2; multi-run single-paragraph, fit policies, truncate ellipsis, fa+arab digits incl. exact-match, measured line-height metrics). line_height deferred: ARC-TPL-053, tracked below | src/arcavex/services/text/service.py, expressions.py | tests/unit/test_text_fit.py, test_locales.py | ipen fa shaping/digits | phase-02 remediation | — |
| §4.3b | `line_height` honored (strut override) | Deferred by specification | — | — | — | rejected with ARC-TPL-053; skia-python 144 StrutStyle exposes no height override (ADR-0001) | skia-python binding |
| §4.4 | Effect system + built-in effects | Tested (Phase 3; category-aware plan geometry→fused color→raster→composite, seeded RNG tree via sha256, paint_bounds from declared expansion; all 15 v1 built-ins incl. SkSL halftone; color matrix fusion proven equivalent; RenderOptions.collect_plan debug hook). Phase-03 remediation: effect lengths accept px (validated against canvas DPI); invalid-param errors list every field; `effects list`/`effect inspect` schema discovery; ink-bleed fixed to grow dark ink (was a dilate growing light) with no bounds expansion. Composite `backdrop` is a deferred accessor returning None in v1 (backlog CR-1). | src/arcavex/builtin/effects_core/, builtin/backend_skia/pipeline.py, backend.py | tests/unit/test_effects.py, tests/golden/test_goldens.py | pop-art grid render, 14+ effect goldens, bounds honesty | phase-03 remediation | — |
| §4.5 | Skia renderer backend, masks, surface pool | Tested (Phase 2 masks; Phase 3 adds per-node offscreen effect compositing, per-render surface pool keyed by (w,h) with reuse + leak guard, shapes_core generators starburst/speech_bubble/qr_code) | src/arcavex/builtin/backend_skia/, builtin/masks_core/, builtin/shapes_core/ | tests/unit/test_masks.py, test_effects.py, test_shapes.py | pool reuse/leak test, shape renders | phase-03 remediation | — |
| §4.6 | Exporters: PNG/JPEG/WebP/PDF | Tested (Phase 7). PNG/JPEG/WebP raster exporters share one encode+atomic-write helper (JPEG flattens alpha over white; WebP lossless is bit-exact VP8L via q100). Raster-embedded RGB PDF exporter: correct physical page size + `TrimBox`/`BleedBox` (A4+bleed first-class), full-res embedded raster, stable metadata only (engine version `/Producer`, render signature `/Subject`, sRGB `/Title`) — no `/CreationDate`/`/ModDate`/`/ID`. skia's PDF rounds MediaBox to integer points, so the page dict is post-processed to the exact fractional boxes and the classic xref is rebuilt (validated by re-parsing offsets; opens in PDFium). Exporter chosen by output extension (`.png/.jpg/.jpeg/.webp/.pdf`); `--quality`/`--lossless` CLI flags (lossy quality defaults to 90, not 100 — DX-4); `ExportOptions` carries quality/lossless/page geometry/engine version. All formats byte-identical on rerun. Phase-7 remediation: a filesystem failure publishing the output (parent is a file, unwritable dir, full disk) is caught and re-raised as a located `ARC-EXP-001` (exit 1), never leaking as `ARC-INT-999` (DX-2); a catalog reachability test now guards that every registered code has a raise-site or a documented exemption. | src/arcavex/builtin/export_raster/{encode,png,jpeg,webp}.py, builtin/export_pdf/{pdf,boxes}.py, kernel/api.py, clients/cli.py | tests/unit/test_export_formats.py, tests/e2e/test_export_cli.py | jpg/webp/pdf CLI renders + PDFium view; per-format sha determinism; A4+bleed box geometry | Phase 7 | — |
| §4.7 | Asset system: CAS, sidecars, decode guards, derived cache | Tested (Phase 4; ingest-by-sha256 into objects/ab/cdef…, JSON sidecar mime/dims/annotations, idempotent+atomic insertion, header-based decode guards enforced before decode: max source bytes, max decoded pixels, format allowlist — PNG/JPEG/GIF/BMP/WEBP header probes). Phase 7 lands the derived-variant LRU cache: downscaled variants keyed by `(content-hash, target-size)` under a configurable byte budget (`[cache].derived_bytes`), memory + disk tiers under `$ARCAVEX_HOME/cache/derived/`, both bounded by the byte budget and LRU-evicted when over it (Phase-7 remediation CR-2: the disk tier now prunes oldest-by-mtime so `cache/derived/` cannot grow unbounded), disposable and never authoritative — a cold cache produces byte-identical output (normalized through the disk PNG so memory and disk hits draw identically). Wired into the Skia image path behind a conservative gate (source ≥4 MP AND ≥4× the slot) so it only engages for a real downscale and never alters existing small-image renders/goldens; source dims from the header probe (no full decode). Used by run manifests to pin assets by hash. | src/arcavex/services/assets/, services/cache/derived.py, builtin/backend_skia/backend.py | tests/unit/test_assets.py, tests/unit/test_derived_cache.py | ingest/guards; cache cold==warm render identity, LRU eviction, disk reuse | Phase 4 + Phase 7 | — |
| §5 | Projects, library, provenance, runs, rerun, diff, versioning | Tested (Phase 4). Library: immutable `templates/<name>/<version>/` + index.toml + lock-protected publish; bare name needs explicit default (never silent latest). Projects: project.yaml manifest, upward discovery + `--project` (no global active state), create/status/clone/set-status, `overrides/<template>.patch.yaml` as the last patch layer. Provenance: `outputs/<ts>_<hash>/manifest.json` pins engine/platform/ir_version/template+style+data hashes/resolved snapshot/asset+font hashes/seed/options/outputs; PNG bytes carry no time/run id. Rerun byte-identical on same engine+platform (refuses exact claim otherwise). Diff = pixel/SSIM + separate template/data/asset/font/engine/option changes. Batch = process-isolated jobs, parallel == serial. Upgrade previews structural stale paths + perceptual diff, updates pin only on --yes. Phase-4 remediation: project override patch is a hashed manifest input (diff attributes it via `overrides`/`overrides.hash`; rerun names on-disk drift via `ARC-RUN-002` + `reproduction.json input_drift`); diff reports per-asset changes; upgrade renders old/target previews (drops a stale override to still preview) + structural node diff + node-path stale reporting; `validate`/`preview` gain project mode; `list-runs --path` lists direct-mode runs; §6.3 config precedence (CLI→ARCAVEX_DPI→project.yaml→config.toml→default) with `doctor` source; manifest `timings_ms` populated; batch ok/failed summary. | src/arcavex/services/{library,projects,orchestrator,runs,config,fsutil,imaging}.py, assets/, kernel/api.py, clients/cli.py | tests/unit/test_{library,projects,assets,config}.py, tests/e2e/test_{provenance,batch,cli_projects,phase04_remediation}.py | full CLI flow: publish→new→validate→render→list-runs→rerun(byte-identical)→diff(patch/asset attributed)→upgrade(previews+diff)→batch(serial==parallel) | Phase 4 + remediation | — |
| §6.1 | CLI (direct + project modes, JSON, exit codes) | Tested (Phase 2 direct mode; Phase 4 adds project mode: `project new/clone/set-status/upgrade`, `status`, project-aware `render` (+ `--record` for direct), `list-runs`, `rerun`, `diff`, `batch --jobs`, `template publish/detach`. Discovery walks up to project.yaml; `--project` overrides; all support `--json`.) | src/arcavex/clients/cli.py | tests/e2e/test_cli*.py, tests/e2e/test_cli_projects.py | acceptance commands (§6.1.2) | phase-02 + Phase 4 | — |
| §6.1.1 | Layout inspection + debug overlay | Accepted (Phase 2; per-node bounds pt/px, absolute_transform, paint_bounds_px, anchor derivation incl. RTL-correct, overlaps via AABB w/ backdrop suppression, free-regions, deconflicted debug labels) | src/arcavex/kernel/api.py, clients/cli.py, builtin/backend_skia | tests/unit/test_inspect_layout.py, tests/e2e/test_cli_layout.py | layout inspect --json | phase-02 remediation | — |
| §6.3 | DX contract (watch preview, inference rules, naming) | Accepted (Phase 2; default name `<stem>.<format>[.<locale>].png`, inference reporting incl. data overlays, versioned JSON) | src/arcavex/kernel/api.py, clients/cli.py | tests/e2e | render naming/inference | phase-02 remediation | — |
| §6.2 | MCP server | Tested (Phase 5). Local stdio FastMCP server (`arcavex mcp serve`) exposing a 19-tool v1 catalog that MIRRORS the service API — every tool is a thin bound-method wrapper delegating to exactly one `kernel.api.Facade` method and returning the SAME versioned pydantic model (schema-parity test asserts each tool's outputSchema == the facade model's JSON schema; no parallel schema). Tools: template list/inspect/validate/patch, project create/list/status/clone, data set/import, asset add/annotate, render_preview (returns MCP image content directly + a structured PreviewResult, `debug=true` overlay), layout_inspect, render, run list/diff/rerun, diagnostic_explain. New facade methods added (reachable via CLI/API too): `patch_template` (ruamel round-trip preserving comments, changed-on-disk detection via `base_sha256`→ARC-TPL-110, unknown-path→located ARC-TPL-092, reuses the §4.1.4 patch grammar on the template's own root AST), `set_data`/`import_data` (project data write/overlay-merge + compile-validate; empty/non-mapping keypath→ARC-TPL-111), `add_asset`/`annotate_asset` (workspace CAS ingest + sidecar annotations), `list_templates`/`list_projects`. `arcavex mcp tools --json` prints the catalog for discovery. stdio only (no network); no MCP-only capability; no extension-authoring tools (deferred). | src/arcavex/clients/mcp_server.py, kernel/api.py, services/{authoring,orchestrator}.py, clients/cli.py | tests/mcp_sessions/ (full inspect→patch→validate→preview→inspect_layout→render loop, create-and-correct exit scenario via direct calls + live `call_tool`, schema parity) | `arcavex mcp tools --json` (19 tools), scripted agent session, seed-broken→correct→render | Phase 5 | — |
| §6.3 | DX contract (watch preview, inference rules, naming) | Not started | | | | | |
| §7 | Trusted local extension SDK + workflow | Tested (Phase 6). `arcavex.sdk` is the one public extension surface (SPI ABCs, effect contexts, color transforms, seeded `effect_rng`, `SurfacePool`/`render_to_pool`/raster helpers, `Points`/`RGBAColor` param fields, IR value types, `COMPONENT_KINDS`/`register_component`, `GoldenHarness`) — it now *owns* the effect runtime types and `builtin.effects_core.context`/`params` re-export from it, so the class an extension imports is the exact one the backend constructs (identity-checked). `extension.toml` multi-component manifest (name/version/ir_min/engine_min + `[[components]]` kind/name/`module:Class`). Custom directory loader imports entry modules under a synthetic `arcavex_ext_<name>` package and registers into the same registries as built-ins; wired into `build_registries` before the solver/backend maps snapshot, so a loaded effect is in the pipeline exactly like a built-in. State at `$ARCAVEX_HOME/extensions/{sources/<name>/,state.toml}`, lifecycle add→validate→disabled→enable (lock-protected), disable takes effect next start. Validator gates: manifest shape, engine/IR compat, AST import-surface scan (only `arcavex.sdk`), determinism lint (`random`/wall-clock/fs read in component methods), pydantic param-schema, contract+name match, SkSL compile. `ext scaffold/validate/test/add/enable/disable/list` CLI + facade methods; `ext test` runs the shipped `golden_test.py` in a crash-contained subprocess. GoldenHarness checks output + bounds-expansion honesty (measures visible outward spread vs declared, fails ARC-EXT-050 on under-declaration). Reference extension `examples/extensions/paper-texture/` (raster effect, committed golden) scaffolds→validates→tests→adds→enables→renders through a template with NO core change. Trust boundary documented honestly (trusted local code, NO sandbox claim; §7.4 deferred untrusted runtime noted). MCP extension-authoring deferred (§6.2). New ARC-EXT-010..060 diagnostics. | src/arcavex/sdk/, src/arcavex/services/extensions/, bootstrap.py, kernel/api.py, clients/cli.py, examples/extensions/paper-texture/, docs/extension-guide.md | tests/unit/test_sdk.py, tests/unit/test_extensions.py, tests/e2e/test_extensions_e2e.py | scaffold→validate→test→add→enable→render acceptance; seeded failures (disallowed import, determinism, dup name, incompat, bad schema, shader, dishonest bounds) | Phase 6 | — |
| §8.3 | Local safety and resource limits | In progress (Phase 4 lands the provenance-adjacent safety: atomic writes via temp+os.replace everywhere (fsutil), cross-process file locks for library publish + index updates, unique run directories for concurrent renders (mkdir-atomic collision suffixing), content-addressed atomic CAS insertion, image decode guards before decode, no global active-project state (per-invocation cwd/`--project` resolution), clean cancellation (staging dir → move only on full success, no partial runs). Immutable completed run dirs enforced behaviorally (engine never reopens a completed run). Expression/repeat/eval budgets already present. Phase 7 completes the per-render budgets: output-dimension/decoded-pixel/surface-memory checked pre-flight from canvas+DPI (refuse before allocation) plus a post-hoc wall-clock guard, all configurable via `[budgets]` and enforced in both the facade and shared pipeline paths, raising located `ARC-RND-020..023` → exit 4. Phase 7 also audits every output writer to the shared atomic `fsutil.atomic_write_bytes` (temp-beside + os.replace), adds an interrupted-write test (prior file intact, no partial), and a template-relative asset path/symlink traversal guard (`ARC-AST-004`).) | src/arcavex/services/{fsutil,runs,library,assets,budgets}.py, orchestrator.py, pipeline.py, kernel/api.py, template/compiler.py | tests/unit/test_budgets.py, test_file_safety.py, tests/e2e/test_export_cli.py | budget exit-4 (CLI + facade), atomic/interrupted write, path traversal | Phase 4 + Phase 7 | — |
| §8.5 | Testing strategy (property, snapshots, golden, e2e, CLI, MCP sessions) | In progress (Phase 3 adds golden-image harness: numpy SSIM/DSSIM ≤ 0.003, per-platform golden dirs, ARCAVEX_UPDATE_GOLDENS + make golden-update, failure heatmap dumps; 14 effect + fusion + halftone + ipen fa/en + pop-art goldens. Property/snapshot/e2e/CLI already present; MCP sessions Phase 5). Phase-03 remediation: pop-art-grid retuned (halftone reads a graded full-tone source; posterize/duotone dropped) and its golden regenerated; ink-bleed/edge-wear goldens strengthened to show the effect. | tests/golden/, tests/property/, tests/layout_snapshots/ | tests/golden/test_goldens.py | goldens pass on regen+recheck | phase-03 remediation | — |
| §4.1.3 | Style packs (palettes, fonts, effect presets, roles; resolution order + provenance) | Tested (Phase 3; name@version + ./file resolution from library-seed/styles + $ARCAVEX_HOME/styles, palettes in expressions, effect_presets, heading/body/accent roles, style→template order, --resolved reports the style layer, style list/inspect). Phase-03 remediation: `--style` CLI override on render/validate/preview/template check (CLI ./file resolves from cwd); ARC-STY-001 now located; style list/inspect JSON carry response_version. | src/arcavex/services/style.py, library-seed/styles/pop-art/0.1.0.yaml, clients/cli.py | tests/unit/test_styles.py, tests/e2e/test_cli.py | style list/inspect --json, --style override, pop-art grid | phase-03 remediation | — |
| §12/spec | Deferred: hostile-extension isolation, WASM, GPU, vector PDF/CMYK, animation, hosted concerns | Deferred by specification | — | — | — | — | — |
| Task §9 | Reference poster bilingual template + 5 formats × 2 locales | Tested (dedicated task after Phase 7). examples/reference-poster/: split template (schema/formats/locales/preview-data sidecars), keyed `repeat` guests in a centred hstack (auto-distributes at any width, RTL flow under fa), diamond-grid hero, circle-masked portraits, exactly two centred info blocks (date زمان / venue مکان — operator-directed simplification from the reference's 3 boxes), per-format reflow patches (square/landscape: venue beside date; story: 2-line 80pt display + deeper band; a4: print-scale point sizes), rotated band accent anchored to the band's top edge so the diagonal lands on every canvas without per-format offsets. Locale correctness hard-won: NO fa font override (blanket Inter→Vazirmatn restyled always-English runs — operator caught), Vazirmatn-first names/digits readable in both scripts, one-line shrink_to_fit headline (story re-enables 2). 12 curated finals (10 PNG + 2 A4 PDF) in outputs/final/. Visual design review (Sonnet, fresh eyes): 10/10 PASS, 0 P1/P2; P3 (two-line venue like the reference) applied. | examples/reference-poster/ | tests/e2e/test_reference_poster.py (render all 10, layout hygiene incl. no clip/truncate + content-collision rule, determinism, preview-data standalone) | 10 renders viewed by orchestrator across 3 iterations + operator feedback round + design matrix docs/agent-runs/reference-poster-design-matrix.md | design review 2026-07-16 | Guest hstack order mirrors under RTL by design; accent AABB overlaps in layout-inspect are rotation artifacts, visually clear (documented in test) |
| Task §12 | Documentation set | Tested (Phase 7 + consolidation pass post-§9). Phase 7: CHANGELOG.md (curated v0.1.0), docs/performance.md (MEASURED actuals vs §8.2 with honest miss analysis), docs/testing.md (layers + per-platform golden strategy + CI matrix), docs/packaged-install.md (clean-venv transcript + ICU/fonts handling), benchmark harness scripts/benchmark.py, .github/workflows/ci.yml. Consolidation pass (2026-07-17): docs index in README + reference-poster showcase (10-finals table + 2 A4 PDFs); created install.md, quick-start.md, template-schema.md (full authoring reference incl. 9 expression fns + 15 effects generated from code), cli.md (all commands, real --help tree), diagnostics.md (index over the 142 generated code docs — count verified == catalog), architecture.md (layer map, pipeline, 8 SPI contracts, 5 import-linter contracts, determinism/provenance), contributing.md, known-limitations.md (honest deferrals + perf misses), adr/README.md, tutorials/ (build-from-scratch, bilingual via reference-poster, extensions); extension-guide.md gained a real scaffold walkthrough. ~40 documented commands executed against the real CLI; all relative md links verified resolving. | CHANGELOG.md, README.md, docs/ (see docs/agent-runs/docs-report.md coverage table), scripts/benchmark.py, .github/workflows/ci.yml | tests/e2e/test_packaged_install.py | wheel clean-venv install; ~40 doc-command transcripts real; link scan clean | docs consolidation 2026-07-17 (orchestrator-verified) | — |
