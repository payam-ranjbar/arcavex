"""Built-in component implementations distributed inside the Arcavex package.

Built-ins obey the same contracts as external extensions but need not be separately
installed (spec §3.3). The kernel never imports this package; ``bootstrap`` does.
"""
