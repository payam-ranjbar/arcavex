# ARC-MCP-011 — Missing required tool argument

An MCP tool call omitted an argument the tool requires, so there is nothing to act on.

**Typical fix:** Pass the named argument; the hint lists the tool's required and optional arguments with what each one means.
