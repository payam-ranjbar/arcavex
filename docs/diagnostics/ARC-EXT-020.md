# ARC-EXT-020 — Extension incompatible with this build

The extension's declared engine_min or ir_min is newer than this engine or IR version, so loading it against this build is not sound.

**Typical fix:** Upgrade Arcavex, or lower engine_min/ir_min if the extension truly supports this build.
