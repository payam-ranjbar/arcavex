# ARC-FX-901 — Unknown mask component

A node references a mask component name that is not registered.

**Typical fix:** Use a registered mask (e.g. rounded_rect, circle, diamond_grid), or add the mask as an extension.
