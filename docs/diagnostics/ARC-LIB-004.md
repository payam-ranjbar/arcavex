# ARC-LIB-004 — Invalid library version or index

A version string is malformed, or a template's index.toml is not valid TOML.

**Typical fix:** Use a dotted version such as '1.0.0'; indexes are disposable, so a bad index can be deleted and regenerated by republishing.
