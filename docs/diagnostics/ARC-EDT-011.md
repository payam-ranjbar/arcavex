# ARC-EDT-011 — Nothing to undo or redo

The history is empty in the requested direction, or an external edit moved the project to a revision the history chain has never seen, which closes the line rather than replaying over someone else's work.

**Typical fix:** Check the activity stream for the external change and continue editing forward.
