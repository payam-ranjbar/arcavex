# ARC-EXT-010 — Extension manifest not found

An extension directory has no extension.toml, so there is no manifest to load.

**Typical fix:** Point at a directory containing an extension.toml, or scaffold one with 'arcavex ext scaffold'.
