# ARC-EDT-012 — The project moved under this edit

The transaction was composed against an older project revision, so applying it would overwrite work done in between. Nothing was changed; the files that moved are named so the edit can be made again against the current version.

**Typical fix:** Re-read the project, compose the edit against the current revision, and re-submit.
