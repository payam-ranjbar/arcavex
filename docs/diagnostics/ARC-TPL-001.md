# ARC-TPL-001 — Template file or directory not found

The template path does not exist, or a directory template has no template.yaml.

**Typical fix:** Check the path on the command line; a split template directory needs a template.yaml file.
