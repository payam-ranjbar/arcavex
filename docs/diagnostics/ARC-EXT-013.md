# ARC-EXT-013 — Unknown component kind

A component (or 'ext scaffold') names a kind that is not one of the eight extensible contracts.

**Typical fix:** Use one of: effect, mask, shape, exporter, layout_solver, template_function, backend, decoder.
