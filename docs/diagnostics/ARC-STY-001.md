# ARC-STY-001 — Unknown or missing style pack

The template's 'style:' reference names a library pack that is not installed, a version that does not exist, or a local file that is missing.

**Typical fix:** Use 'style: name@version' matching an installed pack, or a './file.yaml' path relative to the template; the message lists what is available.
