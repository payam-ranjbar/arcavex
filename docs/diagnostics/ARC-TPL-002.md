# ARC-TPL-002 — Invalid YAML syntax

The template or data file is not valid YAML at the reported location.

**Typical fix:** Fix the YAML syntax error (indentation, quoting, or a stray character) at that line.
