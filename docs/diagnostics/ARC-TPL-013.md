# ARC-TPL-013 — 'variables' is not a mapping

The 'variables' section (or schema.yaml) must be a mapping of names to declarations.

**Typical fix:** Write 'variables:' as e.g. 'title: {type: string, required: true}'.
